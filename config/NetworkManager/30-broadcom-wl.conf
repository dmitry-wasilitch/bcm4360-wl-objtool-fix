# Broadcom's legacy wl driver cannot reliably change the interface MAC during
# scan setup. Scope the compatibility exception to this driver only; other
# Wi-Fi devices retain NetworkManager's default scan randomization.
[device-broadcom-wl]
match-device=driver:wl
wifi.scan-rand-mac-address=no
