PATCH[0]="linux-7.2-string-api.patch"
PATCH_MATCH[0]="^7\\.2"

# Run objtool on the open glue translation units even when the kernel uses
# delayed ThinLTO objtool. The final composite still embeds Broadcom's legacy
# closed object and remains an explicitly documented compatibility boundary.
PATCH[1]="linux-7.2-objtool-glue.patch"
PATCH_MATCH[1]="^7\\.2"
